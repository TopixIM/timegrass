# Upgrade Calcit to 0.14.4

- Upgraded Calcit and `@calcit/procs` together to 0.14.4 and aligned all Calcit modules to released tags.
- Updated CI to `setup-calcit` v1.5.0, which downloads verified standalone `calcit` and `caps` binaries, then verifies the project toolchain.
- Migrated `Result` schemas to the current success/error generic order and isolated native date construction/parsing behind typed FFI adapters.
- Replaced unresolved collection method dispatch with concrete functions; both browser and server entries now pass `analyze dynamic-methods --max 0`.
- Removed the bundled Respo guide in favor of installed-version documentation from `calcit docs`.
- Verified Node 24 install, 8 browser tests, 20 server tests, quality baseline, JavaScript generation, production build, WebSocket snapshot delivery, and browser navigation.
- Loaded the real 736,198-byte `storage.cirru` through an isolated copy. The source remained unchanged at SHA-256 `18212a22d8dfdcd9e9f3ecd8cbc7a5fd8975b7644432e4435ea0297a1935b191`.
